<%= form_for([@account, @creative_development]) do |f| %>
  <% if @creative_development.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creative_development.errors.count, "error") %> prohibited this creative_development from being saved:</h2>

      <ul>
      <% @creative_development.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :design_name, "Design Name" %>
  <%= f.text_field :design_name %><br>

  <%= f.label :requested, "Requested" %>
  <%= f.date_field :requested %><br>

  <%= f.label :completed, "Completed" %>
  <%= f.check_box :completed %><br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
